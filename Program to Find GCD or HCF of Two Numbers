def gcd(a, b):

    result = min(a, b)

    while result > 0:
        if a % result == 0 and b % result == 0:
            break
        result -= 1

    return result


if __name__ == '__main__':
    a = 20
    b = 28
    print(gcd(a, b))
